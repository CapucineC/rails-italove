<div class="content-city">
  <div class="cadre">
    <div class="top-info">
        <h1><span><%=@city.name %></span></h1>
    </div>
    <div class= "sous-cadre">
      <div class="sideblock">
        <div class="description-city">
          <%= @city.description %>
          <p class="classNameHere">Par ici ↓</p>
        </div>
      </div>
    <div class="sideblock">
    <div class="map">
      <img class="map-image"
        <%= image_tag (@city.picture)%>
      <p><%= @city.caption %></p>
      </div>
    </div>
  </div>
</div>
<div class="wrapper">
        <% @city.pictures.order(:position).each do |picture| %>
            <div class="<%= picture.css_class %>"><%= image_tag picture.url, alt: "alttext" %></div>
        <% end %>
</div>
</div>
